spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:postgres}:${DB_PORT:5432}/${DB_NAME:transactions_db}
    username: ${DB_USERNAME:txn_user}
    password: ${DB_PASSWORD:txn_pass}
  security:
    oauth2:
      resourceserver:
        jwt:
          jwks-uri: ${JWKS_URI:http://mock-oauth2:8080/default/jwks}
